<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Add a top menu entry for Mail (Gmail) visible to all Portal users -->
    <record id="menu_top_mail" model="website.menu">
        <field name="name">📧 Correo</field>
        <field name="url">/my/mail</field>
        <field name="parent_id" ref="website.main_menu"/>
        <field name="sequence" eval="50"/>
        <field name="group_ids" eval="[(4, ref('base.group_portal'))]"/>
    </record>

    <!-- Top menu entry to Gmail Settings for Portal users -->
    <record id="menu_top_gmail_settings" model="website.menu">
        <field name="name">Gmail Settings</field>
        <field name="url">/my/settings/gmail</field>
        <field name="parent_id" ref="website.main_menu"/>
        <field name="sequence" eval="51"/>
        <field name="group_ids" eval="[(4, ref('base.group_portal'))]"/>
    </record>
</odoo>
